export async function GET(){
  return new Response('Hello world')
}

// le fichier de route est utilisé pour définir les endpoints de votre application web. Les endpoints sont les URLs qui répondent à des requêtes spécifiques. Dans cet exemple, le fichier de route est probablement utilisé pour définir un endpoint GET qui renvoie simplement "Hello world" comme réponse.